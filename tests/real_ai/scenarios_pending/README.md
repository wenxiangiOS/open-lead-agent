# Pending Real AI Scenarios

These scenario skeletons are **not** loaded by default `scripts/run_real_ai_regression.py`.

- Default runner loads: `tests/real_ai/scenarios/*.json`
- Pending draft folder: `tests/real_ai/scenarios_pending/*.json`

Use these files as implementation backlog templates. After assertions are finalized,
move selected files into `tests/real_ai/scenarios/`.
